import Player form player

placeVim(x, y) {
  // put vim in location
}

listenForKeys() {
  useKeyPress((e.key) => {
    check if colon
      // something here
    else check if 'hjkl'
      // walk logic
  }
}

isColonPress() {
  true or false depending on line 10 - possibly a huge challenge
}
